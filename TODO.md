# TODO

- [ ] Understand root cause of Gradle failure (Flutter Gradle plugin apply method).
- [ ] Update Android Gradle files to use declarative `plugins {}` block instead of imperative `apply plugin`/`apply from` for `flutter.gradle`.
- [ ] Ensure Flutter `flutter.gradle` is compatible with current Gradle version used by the project.
- [ ] Clean build caches (`flutter clean`, remove Gradle cache for the project if needed).
- [ ] Run `flutter pub get` and `flutter run` to verify app builds.

